# hyde-js

A static site generator using Node.js.

## Usage

### Development

To start the development server:

```bash
npm start
```

This will serve the site at `http://localhost:4000`.

### Build

To build the static site:

```bash
npm run build
```

The output will be in the `_site` directory.
